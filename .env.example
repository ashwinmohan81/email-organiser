# Google Gemini API key (optional — only needed if using Gemini backend)
# Get one free at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Ollama model to use for local AI (default: mistral)
OLLAMA_MODEL=mistral
